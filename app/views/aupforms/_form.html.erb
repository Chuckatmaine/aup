<%= form_for(@aupform) do |f| %>
  <% if @aupform.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@aupform.errors.count, "error") %> prohibited this aupform from being saved:</h2>

      <ul>
      <% @aupform.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :userid %><br>
    <%= f.number_field :userid %>
  </div>
  <div class="field">
    <%= f.label :aupaccept %><br>
    <%= f.check_box :aupaccept %>
  </div>
  <div class="field">
    <%= f.label :acceptdate %><br>
    <%= f.datetime_select :acceptdate %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
